<h1>Editing post</h1>
 
<%= form_for :clip, url: clip_path(@clip), method: :patch do |f| %>
  <% if @clip.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@clip.errors.count, "error") %> prohibited
      this post from being saved:</h2>
    <ul>
    <% @clip.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>
 
  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
<% end %>
 
<%= link_to 'Back', clips_path %>
<%= link_to "Welcome Page", controller: "welcome" %>
