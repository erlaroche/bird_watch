<h1>All Documentaries</h1>
 
<table>
  <tr>
    <th>Title</th>
    <th>Text</th>
  </tr>
 
  <% @movies.each do |movie| %>
    <tr>
      <td><%= movie.title %></td>
      <td><%= movie.text %></td>
    </tr>
  <% end %>
</table>

<%= link_to 'New Documenary', new_movie_path %>
<%= link_to "Welcome Page", controller: "welcome" %>